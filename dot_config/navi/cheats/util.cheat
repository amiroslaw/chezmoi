% util

; doesn't need to be surrounded by quote mark
$ files: find . -maxdepth 1 -type f --- --expand --multi
$ dirs: ls -1d */ --- --expand --multi
$ paths: ls -1 --- --expand --multi
$ files_recent: find . -maxdepth 1 -type f -printf '%T@ %p\n' | sort -nr | cut -d' ' -f2-
